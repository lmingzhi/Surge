本次更新：

新增：MITM 开关（默认关闭），代替每次运行时的提示

历史更新：

新增：YouTube_Proxy

新增：YOUKU 开关

增加开关：可以自己选择是否加载基于 证书 的规则

支持白名单模式：当在 User Data 的 Delete 模块内添加“baidu”时，释出的规则时会自动删除 (DOMAIN|DOMAIN-SUFFIX|DOMAIN-KEYWORD),*baidu* 所有内容


微信公众号：墙洞说